## Founder Home — 2026-03-26
| Decision      | Choice                  | Reference                     |
|---------------|-------------------------|-------------------------------|
| Background    | `#1E252A`               | branding.md — primary dark surface |
| Heading font  | Helvetica Now Display   | branding.md — display font    |
| Body font     | Geist                   | branding.md — secondary font  |
| Hero layout   | Asymmetric split        | context.md — system-driven, clear narrative |
| Component UI  | Ghost buttons, #197853 (Emerald) accent | branding.md — interactive signals |
| Journal Grid  | Merged Archive + Scroll-triggered light mode | user request — smooth reading transition |
| Philosophy Layout | Staggered large typography + rapid image sequence box | user wireframe request — dynamic and cinematic feel |
| Philosophy Refinements | 100vh constraints, text masking animations, scaled typography | user request — precise layout fitting and premium reveal motion |
| Authority Section | Converted to light mode (bg-#f4f4f5, text-carbon) and added scroll-triggered Animated Counters using Framer Motion | user request — light mode aesthetic & animated counting |
| Contact Section CTA | Scaled 'Ship what's next' down to a single line, consolidated left/right layouts with a single 'Contact us' button. Section height reduced to 50vh. WebGL uses 20 stacked PlaneGeometries with `alphaTest=0.5` mapping `LogoIcon.png` to perfectly extrude the logo silhouette in 3.5D ASCII on an Emerald background. | user request — explicit true shape logo spinning without rectangular boundaries, concise typography, compact section height |
| Article Page | Created new dynamic routing structure (`/articles/[slug]`). Forced light mode (`#f4f4f5`), staggered typographic reveals, `prose` typography matching the core page (`Geist` + `Helvetica Now Display`), ending with the green ASCII CTA. | user request — cohesive deep reading environment replicating the founder page aesthetic |
| Content Pipeline | Migrated from hardcoded arrays to a production-grade Local MDX file system using `gray-matter` for YAML parsing and `marked` for HTML generation via Next.js `generateStaticParams`. | scale — allowing direct markdown authorship in repo without external CMS dependencies |
| Inquiry Section | Added a brutalist dual-column form (`InquirySection.tsx`) mounted directly above the green Action/ContactSection. Features precise social routing anchors on the left and bordered, transparent text inputs on the right (`bg-[#f4f4f5]`). | user request — dedicated contact data-intake layer before the final CTA |
| Menu Overlay | Implemented `<MenuOverlay />` mounted globally in `<RootLayout />`. Listens natively to `window.dispatchEvent("TOGGLE_MENU")` allowing the header actions to dynamically summon a deeply animated, full-screen carbon takeover menu. Formatted explicitly with pure minimalist layout geometries. Cross-page routing triggers Native browser `.hash` alignment through injected DOM `id` elements tracking explicitly to `#manifesto`, `#journal`, and `#contact`. | user request — enable the MENU text-links to drive site navigation |
| Button Primitives | Systematically standardized all disparate CTAs (Let's Talk, Contact us, Send Dispatch, Read the Dispatch) to identical geometries (`rounded-full`, `font-mono tracking-widest`, tight horizontal padding) enforcing a high-end, unified interactive language. Fixed broken grouping configurations in the global Header arrays to utilize native Flex `gap-8` for rigid CTA distribution. | user request — solve button consistency |
| Global Contact Routing | Built a dedicated `/contact` route featuring a pristine `ContactForm.tsx` stripped of the Founder's personal ecosystem links. Redirected all global "LET'S TALK" and "Contact us" buttons to this monolithic dark-mode intake flow. Removed the redundant `InquirySection` from the Article reading experience and the `READ THE DISPATCH` link from the Hero to hyper-focus user intent mapping. | user request — create a general team intake pipeline and reduce founder-specific external ecosystem hooks |
| Horizontal Scroll Architecture | Shattered the vertical flex of the top 3 featured articles in `EditorialGrid.tsx`, mapping them entirely down a 300vh frame constraint. Escaped the `max-w-7xl` boundaries with full CSS translation to deploy an edge-to-edge sticky window, hooking X-translation exactly from `0%` to `-66.666%` across `framer-motion`'s `useScroll` vector. To resolve intersection clipping on the horizontal plane, created explicit `<motion.div>` subsets for Images and Text, tracking `whileInView` entry directly against the `stickyRef` boundaries for highly cinematic decoupled stagger behavior per article. | user request — make the top articles scroll sideways with staggered internal element entry animations |
